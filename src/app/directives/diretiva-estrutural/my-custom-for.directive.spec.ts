import { MyCustomForDirective } from './my-custom-for.directive';

describe('MyCustomForDirective', () => {
  it('should create an instance', () => {
    const directive = new MyCustomForDirective();
    expect(directive).toBeTruthy();
  });
});
